{
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.17.3",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "17.5.0"
  }
}
