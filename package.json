{
  "scripts": {
    "start": "cd client && npm install && npm run build",
    "test": "node server.js"
  },
  "dependencies": {
    "ejs": "^3.1.7",
    "express": "^4.17.3",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "17.5.0"
  }
}
